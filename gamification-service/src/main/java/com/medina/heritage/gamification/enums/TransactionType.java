package com.medina.heritage.gamification.enums;

/**
 * Type de transaction de points.
 */
public enum TransactionType {
    /**
     * Crédit de points (gain).
     */
    CREDIT,
    
    /**
     * Débit de points (dépense).
     */
    DEBIT
}
